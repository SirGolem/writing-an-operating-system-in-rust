{
    "devDependencies": {
        "prettier": "^3.9.6"
    },
    "scripts": {
        "format": "cargo fmt && prettier . --write",
        "refresh": "cargo clean && pnpm clean && cargo update && pnpm update && pnpm format && cargo check"
    }
}
